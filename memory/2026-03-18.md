
## Session: March 18, 2026 — 03:00+ (Post-compaction)

### Critical Decisions

**Pricing Model CORRECTED**
- Removed tier-based multipliers (0.8x-1.75x) - WRONG approach
- Flat 2.5% platform fee only — agent keeps 97.5%
- TAP score is VISIBILITY/TRUST SIGNAL only — does NOT affect pricing
- Agents set their own rates freely — market decides fair price
- High-rep agents can charge more because hirers trust them, not because we force it

**No Blockchain, No Crypto**
- Pure web stack: Next.js + Supabase
- Traditional payments (Stripe/fiat) only
- Removed all blockchain/crypto references from actual code
- BLS signatures are stubs for future audit, not crypto integration

### Completed

**BLS Signatures (lib/crypto/bls.ts)**
- Stub implementation for BLS12-381
- Key generation, signing, aggregation
- Status reporting for transparency

**TAP SDK (@moltos/tap-sdk)**
- Full npm package structure
- TAPClient with attestations, scores, Arbitra
- Crypto module with stub BLS
- Ready to publish

**package-lock.json**
- Committed for faster CI builds
- TypeScript compiles clean
- Ready for Vercel deploy

### Next (Per User Order)
1. Deploy to Vercel (pending approval)
2. Arbitra dispute resolution flow

### Key Files
- lib/payments/pricing.ts — flat fee logic
- lib/crypto/bls.ts — BLS stubs
- tap-sdk/ — npm package

---

## Session: March 18, 2026 — Later

### Vercel Deployment Debugging

**Status:** Build failing with TypeScript errors

**Investigation:**
- Connected Vercel CLI with token `[REDACTED]`
- Found project `molt.os` (prj_w1vDOxjqspAhy7I4x4DsnmS1wGnx) with rootDirectory: `tap-dashboard`
- All 20+ recent deployments failing with `lint_or_type_error`

**Fixes Applied:**
1. Fixed `lib/eigentrust.ts:481` — Added type assertion for `tap_score` data (was `never` type)
   - Commit: `26364479`

**Next Error:**
- Line 525 in eigentrust.ts — Property access issue on attestation objects
- Need to add type assertions for `agent_id` and `target_id`

**Lesson:** Multiple TypeScript type errors in eigentrust.ts from Supabase query results without proper type annotations.

